<%- include('partials/header', { 
  pageTitle: 'Authentication Successful'
}) %>
<body class="centered">
  <div style="flex-grow:1">
    <div class="container success-container">
      <h1>✓ Authentication Successful!</h1>
      <p>You're all set! Ready to export your liked songs?</p>
      <div class="info-box">
        <p class="info-text">🎵 We'll fetch all your liked songs from Spotify</p>
        <p class="info-subtext">This may take a moment for large libraries</p>
      </div>
      <button class="start-btn" onclick="startFetch()">
        Start Export
      </button>
    </div>
  </div>
  <script>
    function startFetch() {
      // Disable button to prevent double-clicks
      const btn = document.querySelector('.start-btn');
      btn.disabled = true;
      btn.textContent = 'Starting...';
      
      // Redirect to start-fetch endpoint
      window.location.href = '/start-fetch';
    }
  </script>
<%- include('partials/footer') %>
